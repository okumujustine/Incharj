import{_ as e,o as a,c as i,ag as t}from"./chunks/framework.DAvaMl8U.js";const c=JSON.parse('{"title":"Authentication","description":"","frontmatter":{},"headers":[],"relativePath":"auth.md","filePath":"auth.md"}'),n={name:"auth.md"};function h(l,s,r,p,o,d){return a(),i("div",null,[...s[0]||(s[0]=[t(`<h1 id="authentication" tabindex="-1">Authentication <a class="header-anchor" href="#authentication" aria-label="Permalink to &quot;Authentication&quot;">​</a></h1><p>Platform auth (user login) and connector auth (OAuth to external services) are separate concerns. Neither depends on the other.</p><hr><h2 id="user-auth-—-token-flow" tabindex="-1">User auth — token flow <a class="header-anchor" href="#user-auth-—-token-flow" aria-label="Permalink to &quot;User auth — token flow&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>POST /auth/login  { email, password }</span></span>
<span class="line"><span>  │</span></span>
<span class="line"><span>  ├─► access_token  (JWT, 15 min)     → response body</span></span>
<span class="line"><span>  └─► refresh_token (opaque, 30 days) → httpOnly cookie</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Every request: Authorization: Bearer &lt;access_token&gt;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>On 401:</span></span>
<span class="line"><span>  POST /auth/refresh  ← browser sends cookie automatically</span></span>
<span class="line"><span>    │</span></span>
<span class="line"><span>    ├─► new access_token</span></span>
<span class="line"><span>    └─► rotated refresh_token cookie  (old session row deleted)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>POST /auth/logout → cookie cleared + session deleted</span></span></code></pre></div><h3 id="access-token-jwt" tabindex="-1">Access token (JWT) <a class="header-anchor" href="#access-token-jwt" aria-label="Permalink to &quot;Access token (JWT)&quot;">​</a></h3><p>Signed with <code>APP_SECRET</code> using <code>HS256</code> via the <code>jose</code> library. The payload contains only what the middleware needs — no roles, no permissions, no org membership:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;sub&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;user-uuid&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;email&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;alice@example.com&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Alice&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;iat&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1710000000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;exp&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1710000900</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The JWT is short-lived (15 minutes) so a compromised token has a narrow window. It is never stored server-side — the signature verifies it.</p><p>Verification in <code>middleware/auth.ts</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">payload</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> jwtVerify</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(token, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TextEncoder</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">().</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">encode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">APP_SECRET</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// payload.sub is the userId</span></span></code></pre></div><p>If the signature is invalid or the token is expired, <code>jwtVerify</code> throws and the middleware returns 401.</p><h3 id="refresh-token" tabindex="-1">Refresh token <a class="header-anchor" href="#refresh-token" aria-label="Permalink to &quot;Refresh token&quot;">​</a></h3><p>A cryptographically random opaque string (128-bit, hex-encoded). It is stored in the <code>sessions</code> table and in an <code>httpOnly</code>, <code>SameSite=lax</code> cookie.</p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">CREATE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> TABLE</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> sessions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  id           UUID </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">PRIMARY KEY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> DEFAULT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> gen_random_uuid(),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  user_id      UUID </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">NOT NULL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> REFERENCES</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> users(id) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ON DELETE CASCADE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  token_hash   </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">TEXT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> NOT NULL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> UNIQUE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- SHA-256 of the raw token</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  expires_at   </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">TIMESTAMPTZ</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> NOT NULL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  created_at   </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">TIMESTAMPTZ</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> NOT NULL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> DEFAULT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> now</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>The raw token is sent to the browser. Only its SHA-256 hash is stored in the database — if the database is compromised, the hashes cannot be reversed to valid tokens.</p><p><strong>Rotation on every use:</strong> When <code>POST /auth/refresh</code> is called, the server:</p><ol><li>Reads the cookie, hashes the token, looks up the session row</li><li>Checks <code>expires_at</code> — returns 401 if expired</li><li>Deletes the old session row</li><li>Issues a new access token + new refresh token (new session row)</li><li>Sets the new token in the cookie</li></ol><p>If a refresh token is stolen and used before the legitimate user&#39;s next refresh, the attacker gets a new token. But the legitimate user&#39;s next refresh request will fail (the old session was deleted), which forces a logout. The stolen token cannot be reused because rotation deleted the session it corresponded to.</p><hr><h2 id="request-middleware" tabindex="-1">Request middleware <a class="header-anchor" href="#request-middleware" aria-label="Permalink to &quot;Request middleware&quot;">​</a></h2><p>All protected routes use the same two-step check:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 1. Verify JWT, load user</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">api.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;/orgs/:slug/connectors&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, { preHandler: requireCurrentUser }, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">req</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> user</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> getCurrentUser</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(req)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 2. Verify org membership + role</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> membership</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> getCurrentMembership</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(slug, user.id)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  requireRole</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(membership, [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;owner&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;admin&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">])</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Function</th><th>What it does</th></tr></thead><tbody><tr><td><code>requireCurrentUser</code></td><td>Verifies JWT, loads user row from DB, attaches to <code>request.user</code>. Throws 401 if missing or invalid.</td></tr><tr><td><code>getCurrentUser</code></td><td>Reads the already-attached user from <code>request.user</code>. Throws if called before <code>requireCurrentUser</code>.</td></tr><tr><td><code>getCurrentMembership</code></td><td>Confirms the user is a member of the org in the URL slug. Throws 403 if not. Returns the <code>memberships</code> row including the role.</td></tr><tr><td><code>requireRole</code></td><td>Throws 403 if <code>membership.role</code> is not in the allowed list.</td></tr></tbody></table><h3 id="why-load-the-user-from-db-on-every-request" tabindex="-1">Why load the user from DB on every request <a class="header-anchor" href="#why-load-the-user-from-db-on-every-request" aria-label="Permalink to &quot;Why load the user from DB on every request&quot;">​</a></h3><p>The JWT contains <code>sub: userId</code>. The middleware does a DB lookup on every request to ensure the user still exists and is active. If an account is deleted or deactivated, their JWT remains mathematically valid until it expires — the DB check catches this case.</p><p>The lookup is on the primary key (<code>WHERE id = $1</code>) with no joins — it&#39;s effectively free at the DB level.</p><h3 id="roles" tabindex="-1">Roles <a class="header-anchor" href="#roles" aria-label="Permalink to &quot;Roles&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Role</th><th>Permissions</th></tr></thead><tbody><tr><td><code>owner</code></td><td>Everything, including deleting the org and transferring ownership</td></tr><tr><td><code>admin</code></td><td>Manage connectors (create, pause, delete, sync), manage members</td></tr><tr><td><code>member</code></td><td>Read-only: search, browse documents, view connector status</td></tr></tbody></table><p>Roles are enforced per request. There is no caching — a role change takes effect immediately on the next request.</p><hr><h2 id="connector-oauth-—-credential-storage" tabindex="-1">Connector OAuth — credential storage <a class="header-anchor" href="#connector-oauth-—-credential-storage" aria-label="Permalink to &quot;Connector OAuth — credential storage&quot;">​</a></h2><p>OAuth tokens from Google, Notion, and Slack are encrypted before storage.</p><h3 id="oauth-flow" tabindex="-1">OAuth flow <a class="header-anchor" href="#oauth-flow" aria-label="Permalink to &quot;OAuth flow&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. POST /orgs/:slug/connectors</span></span>
<span class="line"><span>      → creates connectors row with has_credentials = false</span></span>
<span class="line"><span></span></span>
<span class="line"><span>2. GET /connectors/:id/oauth/authorize</span></span>
<span class="line"><span>      → backend calls connector.authorizeUrl(state)</span></span>
<span class="line"><span>      → returns consent URL to frontend</span></span>
<span class="line"><span>      → frontend stores { connector_id, org_slug } in localStorage keyed by state param</span></span>
<span class="line"><span></span></span>
<span class="line"><span>3. User approves at Google / Notion / Slack</span></span>
<span class="line"><span></span></span>
<span class="line"><span>4. GET /oauth/&lt;kind&gt;/callback?code=...&amp;state=...</span></span>
<span class="line"><span>      → backend reads state from localStorage map (sent back in query)</span></span>
<span class="line"><span>      → backend calls connector.exchangeCode(code, redirectUri)</span></span>
<span class="line"><span>      → receives { access_token, refresh_token, expiry_date, ... }</span></span>
<span class="line"><span></span></span>
<span class="line"><span>5. encryptCredentials(credentials, ENCRYPTION_KEY)</span></span>
<span class="line"><span>      → stored in connectors.credentials (base64-encoded AES-GCM ciphertext)</span></span>
<span class="line"><span>      → has_credentials = true</span></span></code></pre></div><h3 id="aes-gcm-encryption" tabindex="-1">AES-GCM encryption <a class="header-anchor" href="#aes-gcm-encryption" aria-label="Permalink to &quot;AES-GCM encryption&quot;">​</a></h3><p><code>encryptCredentials</code> in <code>utils/security.ts</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>key         = base64url decode ENCRYPTION_KEY (must be 32 bytes)</span></span>
<span class="line"><span>iv          = 12 random bytes (unique per encryption, stored prepended to ciphertext)</span></span>
<span class="line"><span>ciphertext  = AES-256-GCM encrypt(JSON.stringify(credentials), key, iv)</span></span>
<span class="line"><span>stored      = base64(iv + ciphertext + authTag)</span></span></code></pre></div><p>AES-GCM provides both confidentiality and integrity. The auth tag means tampered ciphertext is detected at decrypt time rather than silently decrypted to garbage.</p><p><code>decryptCredentials</code> reverses this: strips the IV from the front, decrypts, JSON parses. It is called exactly once per sync job, in <code>runner.ts</code>, immediately before the connector uses the credentials.</p><p>Credentials are never:</p><ul><li>Returned by any API endpoint</li><li>Written to logs (the credentials object is never passed to a logger)</li><li>Stored anywhere except <code>connectors.credentials</code></li></ul><h3 id="token-refresh-during-sync" tabindex="-1">Token refresh during sync <a class="header-anchor" href="#token-refresh-during-sync" aria-label="Permalink to &quot;Token refresh during sync&quot;">​</a></h3><p>Before calling <code>listDocuments()</code>, the runner calls <code>connector.refreshCredentials()</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// runner.ts</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> freshCreds</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> connector.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">refreshCredentials</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (freshCreds) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // Re-encrypt and save the updated tokens</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> db.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &#39;UPDATE connectors SET credentials = $1 WHERE id = $2&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    [</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">encryptCredentials</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(freshCreds, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ENCRYPTION_KEY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), connectorId]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  )</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><ul><li><strong>Google Drive</strong>: access tokens expire after 1 hour. <code>refreshCredentials()</code> calls <code>POST https://oauth2.googleapis.com/token</code> with the refresh token. If the refresh token is revoked (user deauthorised the app), it throws — the sync job fails with <code>status = &#39;error&#39;</code>.</li><li><strong>Notion / Slack</strong>: use long-lived tokens that don&#39;t expire. <code>refreshCredentials()</code> returns <code>null</code> (no action needed).</li></ul><hr><h2 id="passwords" tabindex="-1">Passwords <a class="header-anchor" href="#passwords" aria-label="Permalink to &quot;Passwords&quot;">​</a></h2><p>Passwords are hashed with <code>bcryptjs</code> before storage. The cost factor defaults to 12 (2¹² = 4096 iterations), which takes ~300 ms on typical hardware — fast enough for login, slow enough to resist brute-force.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Registration</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> hash</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> bcrypt.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">hash</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(plainPassword, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">12</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> db.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;INSERT INTO users (password_hash, ...) VALUES ($1, ...)&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, [hash])</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Login</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> match</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> bcrypt.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">compare</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(plainPassword, storedHash)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">match) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">throw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> UnauthorizedError</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Invalid credentials&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Plain text never touches the database or logs. Even the Incharj team cannot recover a user&#39;s password — only reset it.</p><hr><h2 id="sessions-table-—-revocation" tabindex="-1">Sessions table — revocation <a class="header-anchor" href="#sessions-table-—-revocation" aria-label="Permalink to &quot;Sessions table — revocation&quot;">​</a></h2><p>Because JWT access tokens cannot be revoked (they&#39;re stateless), forced logout relies on the refresh token lifecycle:</p><ul><li><strong>Logout</strong>: <code>DELETE FROM sessions WHERE id = $1</code> + cookie cleared. The access token remains valid for its remaining lifetime (up to 15 minutes). This is an accepted tradeoff for stateless auth.</li><li><strong>Account deletion</strong>: <code>users.id</code> has <code>ON DELETE CASCADE</code> to <code>sessions</code>. Deleting a user row deletes all their sessions — any refresh attempt immediately fails.</li><li><strong>Expired sessions</strong>: <code>expires_at &lt; now()</code> rows are effectively dead. A background cleanup can <code>DELETE FROM sessions WHERE expires_at &lt; now()</code> periodically without affecting correctness.</li></ul><hr><h2 id="security-summary" tabindex="-1">Security summary <a class="header-anchor" href="#security-summary" aria-label="Permalink to &quot;Security summary&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Concern</th><th>Mechanism</th></tr></thead><tbody><tr><td>Password storage</td><td>bcrypt hash, cost 12</td></tr><tr><td>Access token</td><td>JWT HS256, 15-min expiry, signed with APP_SECRET</td></tr><tr><td>Refresh token</td><td>Opaque random, SHA-256 hashed in DB, httpOnly cookie, rotated on use</td></tr><tr><td>OAuth credentials</td><td>AES-256-GCM with random IV, stored as base64 ciphertext</td></tr><tr><td>Multi-tenant isolation</td><td><code>org_id</code> on every table, enforced at application layer via <code>getCurrentMembership</code></td></tr><tr><td>Token lifetime after logout</td><td>Up to 15 min (access token expiry) — accepted tradeoff for stateless JWT</td></tr></tbody></table>`,58)])])}const E=e(n,[["render",h]]);export{c as __pageData,E as default};
