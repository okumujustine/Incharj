# Database
DATABASE_URL=postgresql://incharj:changeme@localhost:5432/incharj_dev
POSTGRES_DB=incharj_dev
POSTGRES_USER=incharj
POSTGRES_PASSWORD=changeme

# Security — generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
APP_SECRET=changeme-generate-a-real-secret

# Encryption — generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
ENCRYPTION_KEY=changeme-generate-a-real-encryption-key

# URLs
FRONTEND_URL=http://localhost:3000
ENVIRONMENT=development

# Google Drive OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Notion OAuth
NOTION_CLIENT_ID=
NOTION_CLIENT_SECRET=

# Slack OAuth
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=

# OpenAI API — for semantic search embeddings
OPENAI_API_KEY=sk-...
SEMANTIC_SEARCH_ENABLED=true
EMBEDDING_PROVIDER=openai
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIMENSIONS=1536
EMBEDDING_BATCH_SIZE=64
